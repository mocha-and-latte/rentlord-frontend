{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "dev",
      "skipFiles": ["<node_internals>/**"],
      "command": "npm run dev",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
